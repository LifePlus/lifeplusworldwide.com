---
import Container from '../Container.vue'
import ExternalLinkIcon from '../icons/ExternalLinkIcon.vue'
import BlockTitle from '../BlockTitle.astro'
const { content } = Astro.props
const textSize = {
  small: 'text-lg lg:text-xl',
  normal: 'text-xl lg:text-2xl',
  large: 'text-2xl lg:text-4xl',
}
// w-1/2
// mb-4
---

<style>
.centered-content img {
  margin-bottom: theme('spacing.4');
}
</style>

<div class="py-12">
  <Container>
    <BlockTitle content={content} />
    <div class="max-w-[34rem] mx-auto py-12 lg:py-20">
      <div set:html={content.content} class={`${textSize[content.contentTextSize]} centered-content mb-12`}></div>
      {content.callToAction &&
        <a target="_blank" href={content.actionUrl} class={`inline-flex items-center space-x-4 lg:space-x-8 text-2xl lg:text-4xl font-bold`}>
          <span>{content.callToAction}</span>
          <div class="flex-1 h-12 w-12 lg:w-16 lg:h-16 mx-auto flex items-center justify-center rounded-full bg-white text-brand-primary transition">
            <ExternalLinkIcon class="w-5 h-5 fill-current" />
          </div>
        </a>
      }
    </div>
  </Container>
</div>